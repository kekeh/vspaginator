/**
 * @ngdoc object
 * @name vspgConf
 * @description Constants and default values of the vspaginator module.
 */
_vspg.constant('vspgConf', {
    visibleBtnCount: 3,
    prevNextBtn: {
        visible: true,
        labels: ['back', 'next']
    },
    prevNextSetBtn: {
        visible: true,
        labels: ['...', '...']
    },
    firstLastBtn: {
        visible: true,
        labels: ['first', 'last']
    }
});
